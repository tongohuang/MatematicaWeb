/**
 * Este archivo es un placeholder para mantener compatibilidad con código existente.
 * La funcionalidad del editor matemático ha sido reemplazada por clean-text-editor.js.
 */

// Funciones vacías para mantener compatibilidad
window.initNewMathEditor = function() {
    console.log("Editor matemático deshabilitado. Usando editor de texto limpio en su lugar.");
    return false;
};

window.cleanupMathEditor = function() {
    // No hace nada
};

// Evitar que se inicialice automáticamente
document.addEventListener('DOMContentLoaded', function() {
    // No hacer nada
});
